import { Component } from '@angular/core';

@Component({
  selector: 'app-structure-directive-if-else',
  templateUrl: './structure-directive-if-else.component.html',
  styleUrls: ['./structure-directive-if-else.component.scss']
})
export class StructureDirectiveIfElseComponent {
  user = {
    name: "Tiep Phan",
    age: 10,
  };
}
